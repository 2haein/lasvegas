/**
 * @format
 * @flow strict-local
 */

import { StyleSheet } from 'react-native';
const LoginStyle = StyleSheet.create({
    TitleText: {
        color: '#090979',
      },
  });

  export default LoginStyle;